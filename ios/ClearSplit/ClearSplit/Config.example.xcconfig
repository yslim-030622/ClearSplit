// Config.example.xcconfig
// This is a template file committed to version control.
// Copy this to Config.xcconfig and fill in your actual values.
// Config.xcconfig is gitignored and should never be committed.

// API Base URL for the backend
// For simulator: use http://localhost:8000
// For real device: use your Mac's LAN IP, e.g. http://192.168.1.10:8000
API_BASE_URL = http:/$()/localhost:8000


