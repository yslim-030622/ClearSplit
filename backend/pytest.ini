[pytest]
# Use auto mode for async test detection
asyncio_mode = auto

# Function-scoped event loop (one per test)
asyncio_default_fixture_loop_scope = function

# Verbose output, fail fast on errors for quicker debugging
addopts = -v --tb=short

markers =
    asyncio: mark a test as using asyncio
