[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths =
    app/tests
addopts =
    -ra
    --strict-markers
markers =
    asyncio: mark a test as using asyncio
    e2e: end-to-end smoke coverage across backend modules
