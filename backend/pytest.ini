[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Run tests sequentially to avoid connection pool exhaustion
addopts = -p no:xdist
markers =
    asyncio: mark a test as using asyncio
